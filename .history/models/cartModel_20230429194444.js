const path =require('path')

module.exports = class Cart{
    constructor(){
     // intial data   
    }
   const p = path.join(path.dirname(process.mainModule.filename), 'dat')
    addToCart(){
    }
}